Extract device certificate so it can be changed from Main.ts
Close message exchanges
Retry sending a message if there is no ack from the other side
Listen for MDNS queries and reply
Add an integration test to make sure the whole flow is working
Add certificate validation
Add support for brightness level
Add more nodes
Implement ACL
Remove elliptic dependency
Remove BN dependency in favor of BigInt
Add support for WiFi / Bluetooth / Thread commisionning
Add IPv6 support
Make it compatible with a web API to use it in a browser / webview
